// src/environments/environment.dev.ts (local development)
export const environment = {
  production: false,
  apiBase: 'http://localhost:8080',   // Docker backend URL - direct calls without proxy
};